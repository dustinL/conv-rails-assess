<% content_for(:title, "New User | Concert Collector") %>

<div class="container">
  <div class="row">
    <div class="col-xs-6 col-xs-offset-3">
      <%= render("layouts/errors", :object => @user) %>
      <h1 class="text-center">Create New User</h1>
      <%= render 'form' %>
    </div>
  </div>
</div>
