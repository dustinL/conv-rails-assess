<% content_for(:title, "Artists Home | Concert Cal") %>

<div class="row">

    <div class="jumbotron">
      <h3>Your Artists:</h3>
    </div>

  <ul>
    <% @artists.each do |artist| %>
      <h4><%= link_to "#{artist.name}", artist_path(artist.id) %></h4>
      <% end %>
  </ul>
</div>
